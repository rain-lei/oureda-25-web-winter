# 知识点自测

都是前端三件套比较基本和常用的一些知识点，建议食用方法：**先自己填一遍，每个问题再单独查一遍**。

*此部分不建议一上来就使用 AI，最终要求完成即可，重在自我检验。*
*本文为了可读性在代码处采用了 markdown 语法，能理解题意就行。*

## HTML

- Q1：你能说出哪些标签？与表单相关的是哪几个？
  A1：`<p>,<body>,<html>,<h1>,<div>,<img>,<table>`
  `<label>,<inbut>,<select>`
  

- Q2：补全 `style` 属性，使`<div style=""></div>`的宽为 `100px`、高为 `100px`。
  A2: `<div style="width: 100px; height: 100px;"></div>`

## CSS

- Q1：选择器有哪几种分类？
  A1：`基本、组合、属性、文档结构、伪类、伪元素选择器`

- Q2：分别说几种伪类和伪元素？
  A2:伪类：`:hover`,`:active`,`:first-child`。
伪元素：`::before`,`::after`

- Q3：flex 布局和 grid 布局分别有哪些常用属性？
  A3：flex：flex-directiom,justify-content:center(水平居中)，align-item:center(垂直居中)，flex:1,flex-basis,flex-shrink,align-content
grid:auto-fill,auto-fit,align-content,grid-template-column:1fr ,
- Q4：说说盒模型的构成？
  A4：
 ` Margin (外边距) ，Border (边框)，Padding (内边距)，Content (内容)`
## JavaScript

- Q1：JS 中有哪些**基本数据类型**？
  A1：字符串String、数字Number、布尔Boolean、空Null、未定义Undefined、Symbol

- Q2：如何通过 dom 操作获取 `id` 为 `test` 的元素，为其添加一个 "click" 事件？
  A2：
  ```
  const listen = document.getElementById('test');
    if (listen) {
      listen.addEventListener('click', () => {
        console.log("点击了test");
      });
    }
  ```

- Q3：箭头函数对比普通函数的区别有哪些？
  A3：
  1.一次性，用完即止，通常简短
  2.箭头函数this固定指向定义，而普通函数的 this 指向调用对象

- Q4：已知一个变量 `num === 1`，写一个模版字符串，使其显示结果为 `“class1”`，即在 `"class"` 后面插入变量 `num`。
  A4：
  ```
  `class${num}`

  ```
